import React from 'react';

export default function reducer(state, action) {
  switch (action.type) {
    case 'create_board':

    case 'player':

    case 'reset_game':

    default:
      return state;
  }
}
